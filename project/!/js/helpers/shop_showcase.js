$(document).ready(function() {
    let api = new WebAPI();
    let shop = new CurrentShop(shopId).setCurrentShopId(shopId).setCurrentShopId(shopId)
    let user = new CurrentUser();
    mainApplication
     .setUser(user)
     .showCaseOutput(shopId);
});
